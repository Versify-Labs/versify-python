{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "CreateMintLink",
  "required": ["product"],
  "properties": {
    "active": {
      "description": "Whether the link’s url is active. If false, customers visiting the URL will be shown a page saying that the link has been deactivated.",
      "type": "boolean"
    },
    "airdrop": {
      "description": "Airdrop that createde this mint link.",
      "type": "string"
    },
    "after_completion": {
      "additionalProperties": true,
      "description": "Instructions for after the mint is finished.",
      "type": "object"
    },
    "metadata": {
      "additionalProperties": true,
      "description": "Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.",
      "type": "object"
    },
    "name": {
      "description": "The name of the mint link. Used for organizational purposes only.",
      "type": "string"
    },
    "product": {
      "description": "The ID of the product being minted with the link.",
      "type": "string"
    },
    "public_mint": {
      "description": "If false, email minting must be included in whitelist.",
      "type": "boolean"
    },
    "whitelist": {
      "description": "List of emails that are allowed to mint",
      "type": "array"
    }
  }
}
